{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Swagger": {
    "//": "Set to false to disable the Swagger UI / OpenAPI document (e.g. in production).",
    "Enabled": true
  },
  "CloasProxy": {
    "//": "The real CLOAS .svc endpoint this proxy forwards every request to. Change only this value and restart - no rebuild.",
    "TargetServiceUrl": "http://testet.com/cloaservice.svc",
    "TimeoutSeconds": 300
  }
}
